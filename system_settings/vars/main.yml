---
# vars file for system_settings

    packagesSystem:
      - lm-sensors
      - wget
      - curl
      - unzip
      - git
      - python3
      - python3-pip
      - net-tools
      - zsh-autosuggestions

    packagesKVM:
      - qemu-system
      - libvirt-daemon-system
      - qemu-utils
      - virtinst
      - bridge-utils
      - libvirt-daemon
      - libguestfs-tools

    packagesCockpit:
      - cockpit
      - cockpit-machines
      - cockpit-podman

    new_hostname: CITADELDEVELOP
    old_hostname: debian
    temp_dir: /home/vlad/temp
    user: vlad

